class Solution:
    def zeroFilledSubarray(self, nums: List[int]) -> int:
        c=0
        z=0
        for i in range(len(nums)):
            if(nums[i]==0):
                z+=1
                c+=z
            else:
                z=0
        return c
        
