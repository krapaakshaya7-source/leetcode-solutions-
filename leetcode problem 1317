class Solution:
    def getNoZeroIntegers(self, n: int) -> List[int]:
        a=n-1 #101-1 =100
        b=1
        while('0' in str(a) or '0' in str(b)):
            a-=1 # a=100-1    a=99
            b+=1 # b+=1       b=2
        return[a,b]
        
